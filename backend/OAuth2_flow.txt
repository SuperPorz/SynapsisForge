BROWSER                    TUO SERVER              GOOGLE
   │                           │                      │
   │──── click "Login Google" ─▶│                      │
   │                           │                      │
   │◀─── redirect 302 ─────────│                      │
   │         (con client_id,   │                      │
   │          scope, state,    │                      │
   │          redirect_uri)    │                      │
   │                           │                      │
   │──────────────────────────────────────────────────▶│
   │                    (l'utente vede la pagina Google)│
   │                    (e dà il consenso)              │
   │◀──────────────────────────────────────────────────│
   │  redirect verso redirect_uri                      │
   │  ?code=4/ABC123&state=xyz                         │
   │                           │                      │
   │──── GET /auth/callback ───▶│                      │
   │     ?code=4/ABC123        │                      │
   │                           │──── POST /token ─────▶│
   │                           │    { code,           │
   │                           │      client_secret,  │
   │                           │      redirect_uri }  │
   │                           │◀─────────────────────│
   │                           │  { access_token,     │
   │                           │    refresh_token,    │
   │                           │    id_token }        │
   │                           │                      │
   │                           │  [valida id_token,   │
   │                           │   crea/trova utente, │
   │                           │   emette JWT proprio]│
   │◀─── JWT proprio ──────────│                      │
   │   (cookie httpOnly        │                      │
   │    o response body)       │                      │