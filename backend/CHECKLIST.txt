SynapsisForge — Implementation Checklist
==========================================
Legenda: [ ] da fare  [x] fatto  [-] non applicabile per ora

------------------------------------------
AUTH MODULE
------------------------------------------
[ ] POST /auth/register — crea utente con password hashata (bcrypt)
[ ] GET  /auth/verify-email/:token — attiva account dopo registrazione
[ ] POST /auth/login — verifica credenziali, emette access + refresh token
[ ] POST /auth/refresh — emette nuovo access token da cookie refresh
[ ] POST /auth/logout — invalida refresh token (Redis o DB)
[ ] POST /auth/password/reset — invia email con token reset
[ ] POST /auth/password/confirm — aggiorna password con token
[ ] GET  /auth/google — redirect OAuth2 Google
[ ] GET  /auth/google/callback — callback Google, crea utente se non esiste
[ ] GET  /auth/github — redirect OAuth2 GitHub
[ ] GET  /auth/github/callback — callback GitHub, gestisce email assente

------------------------------------------
USERS MODULE
------------------------------------------
[ ] createUser() — chiamato internamente da register e OAuth callback
[ ] findById() — usato da JwtStrategy e altri service
[ ] findByEmail() — usato da AuthService per login
[ ] GET  /users/me — restituisce profilo utente autenticato
[ ] PATCH /users/me — aggiorna first_name, last_name, birth_date, country
[ ] GET  /users/me/enrollments — lista iscrizioni studente con progresso
[ ] GET  /users/me/courses — lista corsi creati dall'instructor autenticato
[ ] GET  /users/me/certificates — lista certificati conseguiti dallo studente

------------------------------------------
COURSES MODULE
------------------------------------------
[ ] GET    /courses — lista con filtri (category, price, search) e paginazione
[ ] GET    /courses/:id — dettaglio corso con lezioni
[ ] POST   /courses — crea corso (instructor)
[ ] PATCH  /courses/:id — aggiorna corso (instructor owner o admin)
[ ] DELETE /courses/:id — soft delete corso (instructor owner o admin)
[ ] findBySlug() — usato internamente per SEO/URL friendly
[ ] Cache su findAll() e findBySlug() con Redis (TTL + invalidation)

------------------------------------------
LESSONS MODULE
------------------------------------------
[ ] GET    /courses/:courseId/lessons/:id — dettaglio lezione (PostgreSQL + MongoDB)
[ ] POST   /courses/:courseId/lessons — aggiunge lezione a corso (instructor)
[ ] PATCH  /courses/:courseId/lessons/:id — aggiorna lezione (instructor)
[ ] DELETE /courses/:courseId/lessons/:id — elimina lezione (instructor o admin)
[ ] GET    /lessons/:id/video-url — genera presigned GET URL S3 (verifica iscrizione)

------------------------------------------
ENROLLMENTS MODULE
------------------------------------------
[x] enroll() — verifica StudentProfile, corso PUBLISHED, pagamento COMPLETED, no duplicato
[x] updateProgress() — marca lezione completata su MongoDB, ricalcola progress_percent
[ ] POST   /enrollments — iscrive studente a corso
[ ] PATCH  /enrollments/:id/progress — aggiorna progresso con lesson_id
[x] EventEmitter2 — emette "enrollment.completed" quando progress_percent = 100
[ ] POST   /enrollments/:id/refund — richiede rimborso (student o admin)

------------------------------------------
PAYMENTS MODULE
------------------------------------------
[ ] GET  /payments/client-token — genera token Braintree per Drop-in UI
[ ] POST /payments/checkout — crea transazione Braintree, salva Payment, crea Enrollment
[ ] POST /payments/subscribe — crea abbonamento mensile Braintree
[ ] GET  /payments/subscription-status — stato abbonamento utente
[ ] POST /payments/cancel-subscription — cancella abbonamento
[ ] GET  /payments/history — storico pagamenti utente autenticato
[ ] POST /payments/webhook — gestisce eventi Braintree (firma verificata)
[ ] Idempotenza webhook — un evento doppio non crea duplicati

------------------------------------------
REVIEWS MODULE
------------------------------------------
[ ] POST   /enrollments/:enrollmentId/reviews — pubblica recensione (solo se enrolled)
[ ] PATCH  /enrollments/:enrollmentId/reviews — modifica propria recensione
[ ] DELETE /enrollments/:enrollmentId/reviews — elimina recensione (student o admin)
[ ] Vincolo una sola recensione per coppia student + course

------------------------------------------
CERTIFICATES MODULE
------------------------------------------
[ ] Listener su "enrollment.completed" — crea record Certificate
[ ] Job BullMQ generate-certificate — genera PDF con pdfkit, carica su S3
[ ] GET /certificates/verify/:code — verifica pubblica autenticità certificato
[ ] GET /certificates/:id/download — presigned GET URL S3 per download PDF

------------------------------------------
ADMIN MODULE
------------------------------------------
[ ] GET   /admin/users — lista tutti gli utenti con filtri e paginazione
[ ] PATCH /admin/courses/:id/approve — approva o rifiuta corso
[ ] GET   /admin/stats — statistiche globali (utenti, corsi, iscrizioni, revenue)
[ ] GET   /admin/cache-stats — metriche Redis
[ ] GET   /admin/jobs/stats — statistiche code BullMQ
[ ] POST  /admin/jobs/weekly-report — trigger manuale report settimanale instructor

------------------------------------------
UPLOADS MODULE
------------------------------------------
[ ] POST /uploads/presigned-url — genera presigned PUT URL S3 per upload video

------------------------------------------
MONGODB — LessonContent Schema
------------------------------------------
[ ] Schema LessonContent: lessonId, videoUrl, transcript, attachments, quiz, external_links
[ ] createContent() — crea documento MongoDB per una lezione
[ ] updateContent() — aggiorna documento MongoDB
[ ] findByLessonId() — usato da LessonsService per fondere dati PG + Mongo

------------------------------------------
MONGODB — LessonProgress Schema
------------------------------------------
[x] Schema LessonProgress: enrollmentId, lessonId, completedAt
[x] upsert lezione completata (atomico, no duplicati)
[x] countDocuments per calcolo progress_percent

------------------------------------------
INFRASTRUTTURA & TRASVERSALI
------------------------------------------
[ ] ValidationPipe globale (whitelist: true, transform: true)
[ ] TransformInterceptor globale — wrappa risposta in { data, statusCode, timestamp }
[ ] LoggingInterceptor — logga method, path, status, tempo risposta
[ ] TimeoutInterceptor — 504 se endpoint non risponde entro 10s
[ ] HttpExceptionFilter globale — standardizza tutti gli errori inclusi TypeORM
[ ] ParseUuidPipe custom — valida UUID v4 su ogni :id param
[ ] ParsePositiveIntPipe — per params di paginazione
[ ] RolesGuard + decoratore @Roles() — RBAC su ogni endpoint protetto
[ ] JwtAuthGuard globale + decoratore @Public() per endpoint pubblici
[ ] Throttler globale con Redis store (rate limiting per IP)
[ ] EventEmitter2 — bus eventi interno (enrollment.completed → certificate)
[ ] BullMQ — code per email, PDF, digest, cleanup
[ ] Redis — caching, refresh token storage, rate limiting, Pub/Sub
[ ] Swagger — ogni endpoint documentato con @ApiOperation, @ApiResponse, @ApiBearerAuth
[ ] Seed script — dati realistici per demo (utenti, corsi, lezioni, pagamenti)