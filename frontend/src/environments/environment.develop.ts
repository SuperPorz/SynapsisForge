export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000'  // URL del backend locale
};

///// quando ci sarà host vero ///
/* export const environment = {
  production: true,
  apiUrl: 'https://api.synapsis.com'  // da verificare in futuro
}; */